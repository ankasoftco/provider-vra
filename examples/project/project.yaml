apiVersion: project.crossplane.io/v1alpha1
kind: Project
metadata:
  name: test-project-1
spec:
  forProvider:
    name: 'test-project-1'
    description: 'test-project-1 - alpcan - caner'

  providerConfigRef:
    name: vra-test
